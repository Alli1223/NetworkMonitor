"""Application version. Bumped on each release tag."""

__version__ = "0.2.0"
APP_NAME = "Network Monitor"
APP_ID = "NetworkMonitor"
ORG_NAME = "NetworkMonitor"

# GitHub repository in "owner/repo" form, used by the auto-updater.
# Override at build time or via environment, but defaults are fine for source runs.
GITHUB_REPO = "Alli1223/NetworkMonitor"
